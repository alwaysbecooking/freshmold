default:
	@just --list

# usage: replace-w-var {{cookiecutter.project_slug}} {{cookiecutter.project_slug}}
replace-w-var from to:
	@echo "Replacing '{{from}}' with '{{to}}'..."
	@rg -l "{{from}}" | xargs sd "{{from}}" "{{to}}"
	@echo "Done."
