default:
	@just --list

# usage: replace-w-var some_value :
replace-w-var from to:
    @echo "Replacing '{{from}}' with '{{to}}'..."
    @rg -l "dummy" | xargs sd "dummy" "{{cookiecutter.project_slug}}"
    @echo "Done."
